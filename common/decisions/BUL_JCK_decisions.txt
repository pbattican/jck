BUL_foreign_policy = {
	BUL_ask_to_join_Reichspakt = {
		icon = generic_political_discourse
		allowed = {
			original_tag = BUL
		}
		available = {
			OR = {
				GER = {
					has_war_with = FRA
				}
				date > 1939.6.1
			}
		}
		visible = {
			country_exists = GER
			GER = {
				is_faction_leader = yes
			}
			GER = {
				is_faction_leader = yes
				NOT = {
					is_in_faction_with = ROOT
					has_war_with = ROOT
				}
			}
		}
		fire_only_once = yes
		cost = 0
		#days_remove = 30
		ai_will_do = {
			factor = 100
		}
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision BUL_join_Reichspakt"
			every_country = {
				limit = {
					OR = {
						is_in_faction_with = BUL
						original_tag = BUL
					}
				}
				GER = {
					add_to_faction = PREV
				}
			}
		}
	}
	BUL_join_RP_tech = {
		icon = generic_research
		allowed = {
			original_tag = BUL
		}
		available = {
			has_country_flag = BUL_intervened_in_weltkrieg
			OR = {
				is_in_faction_with = GER
				is_subject_of = GER
			}
			NOT = {
				is_in_tech_sharing_group = german_research
			}
		}
		visible = {
			original_tag = BUL
		}
		fire_only_once = yes
		cost = 0
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision BUL_join_RP_tech"
			add_to_tech_sharing_group = german_research
		}
	}
}
